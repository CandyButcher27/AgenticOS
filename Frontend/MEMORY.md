# Frontend

Project has its own `CLAUDE.md` — read that first for conventions/architecture. Contains `websites/`, `video_animated/`, `portfolio-concepts/`, `design/`, `scripts/`, plus `graphify-out/` (generated knowledge graph). Has `requirements.txt`, `config.yaml`, `README.md`, `REGENERATE.md`.
