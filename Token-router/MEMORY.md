# Token-router

Project has its own `CLAUDE.md` — read that first. Python app: `app.py`, `chat_core.py`, `catalog.py`/`catalog.yaml`, `config.py`/`config.yaml`, `mcp_server.py`. Notes in `inspirations-token-optimisers.md` and `problems+solutions.md`. `.venv` present, `.python-version` pinned. Recent commits regenerated posthog/replicate/resend configs via the router.
